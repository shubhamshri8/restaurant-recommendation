# Contracts

These are the **stable interfaces** for the project.

- JSON Schemas in `contracts/schemas/`
- Example payloads in `contracts/examples/`

The goal is to keep these stable while we implement Phase 1+ behind them.

